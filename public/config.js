/* ================================================================
   تنظیمات اتصال پلتفرم ویــِـره
   ----------------------------------------------------------------
   این تنها فایلی است که باید ویرایش شود. دو مقدار زیر را از
   داشبورد Supabase → Settings → API بردارید و جایگزین کنید.
   بعد از ویرایش، فقط همین فایل را دوباره آپلود کنید — نیازی به
   build گرفتن یا تغییر هیچ فایل دیگری نیست.
   ================================================================ */
window.__VIVERE_CONFIG__ = {
  // آدرس پروژه — مثال: "https://abcd1234.supabase.co"
  SUPABASE_URL: "REPLACE_WITH_YOUR_SUPABASE_URL",

  // کلید anon / public — با eyJ شروع می‌شود
  SUPABASE_ANON_KEY: "REPLACE_WITH_YOUR_ANON_KEY",
};
